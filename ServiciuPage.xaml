<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Programare_Clinica.ServiciuPage"
             Title="Adauga serviciu">
    <ContentPage.Content>
        <StackLayout Margin="20">
            <Editor Placeholder="Adauga nume serviciu" Margin="20"
 Text="{Binding Nume}"
 HeightRequest="50" />
            <Editor Placeholder="Adauga pret" Margin="20"
 Text="{Binding Pret}"
 HeightRequest="50" />
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Button Text="Save Serviciu" Grid.Column="0"
MaximumWidthRequest="200"
 Clicked="OnSaveButtonClicked" />
                <Button Text="Delete Serviciu" Grid.Column="1"
MaximumWidthRequest="200"
 Clicked="OnDeleteButtonClicked" />
            </Grid>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>